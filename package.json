{
  "name": "agent-training",
  "version": "1.0.0",
  "description": "GitHub Copilot Agent 研修用 Todo API サーバ",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:submit": "vitest run --config vitest.submit.config.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
